services:
  postgres:
    image: postgres:18.6-alpine
    container_name: postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: database
    ports:
      - "5432:5432"
